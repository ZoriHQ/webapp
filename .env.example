# ==============================================
# Zori Web Application - Environment Variables
# ==============================================

# ----------------------------------------------
# Authentication Mode
# ----------------------------------------------
# Options: "clerk" (cloud) or "oss" (self-hosted)
# - clerk: Full-featured auth with Clerk (requires VITE_CLERK_PUBLISHABLE_KEY)
# - oss: Simple static user authentication (no external dependencies)
VITE_AUTH_MODE=oss

# ----------------------------------------------
# API Configuration
# ----------------------------------------------
VITE_API_BASE_URL=http://localhost:1323

# ----------------------------------------------
# Map Configuration
# ----------------------------------------------
# Get your token from: https://account.mapbox.com/access-tokens/
VITE_MAPBOX_ACCESS_TOKEN=your_mapbox_token_here

# ----------------------------------------------
# OSS Mode Configuration
# ----------------------------------------------
# Only used when VITE_AUTH_MODE=oss
# OSS mode uses JWT-based authentication with login/logout flow
# Users will see a login screen and authenticate via API

# ----------------------------------------------
# Clerk Configuration (Cloud Mode)
# ----------------------------------------------
# Only used when VITE_AUTH_MODE=clerk
# Get your keys from: https://dashboard.clerk.com

# VITE_CLERK_PUBLISHABLE_KEY=pk_test_...

# ----------------------------------------------
# Zori Analytics (Cloud Mode Only)
# ----------------------------------------------
# Only used when VITE_AUTH_MODE=clerk
# Analytics tracking is disabled in OSS mode

# VITE_ZORI_PUBLISHABLE_KEY=zori_pt_...

# ----------------------------------------------
# Production Example (.env.production)
# ----------------------------------------------
# VITE_AUTH_MODE=clerk
# VITE_CLERK_PUBLISHABLE_KEY=pk_live_...
# VITE_API_BASE_URL=https://api.yourdomain.com
# VITE_MAPBOX_ACCESS_TOKEN=your_production_token
